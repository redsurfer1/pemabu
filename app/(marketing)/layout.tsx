import { MarketingLayoutShell } from "@/components/home/MarketingLayoutShell";

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <MarketingLayoutShell>{children}</MarketingLayoutShell>;
}
